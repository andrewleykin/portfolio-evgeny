mixin icon(name,mod)
	- mod = mod || ''
	svg(class="icon icon-" + name + ' ' + mod)
		use(xlink:href="app/img/svg/symbol/sprite.svg#" + name)

-
- var getRandomInt = function(min, max) {
-   return Math.random() * (max - min) + min;
- }
